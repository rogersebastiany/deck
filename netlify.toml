[build]
  command = "node generate_cards.js && mkdir -p dist && cp website/index.html website/cards.json dist/ && cp *.webp *.png dist/ && sed -i 's/\\.\\.\\//\\.\\//g' dist/index.html dist/cards.json"
  publish = "dist"
